import React from 'react';
import UserStorySectionView from './UserStory.view';

const UserStory = ({ className }: { className?: string }) => {
  return <UserStorySectionView className={className} />;
};

export default UserStory;

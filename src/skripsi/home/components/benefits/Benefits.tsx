import React from 'react';
import BenefitsSectionView from './Benefits.view';

const Benefits = ({ className }: { className?: string }) => {
  return <BenefitsSectionView className={className} />;
};

export default Benefits;
